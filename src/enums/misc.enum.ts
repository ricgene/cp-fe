export enum SortByEnum {
  OLDEST = "OLDEST",
  NEWEST = "NEWEST",
}

export enum TagEnum {
  BUSINESS = "BUSINESS",
  PRODUCT = "PRODUCT",
  OFFER = "OFFER",
}

export enum DurationFilterEnum {
  ALL = "ALL",
  TODAY = "TODAY",
  TOMORROW = "TOMORROW",
  THIS_WEEK = "THIS_WEEK",
  THIS_MONTH = "THIS_MONTH",
}
