export enum SortByEnum {
  "asc" = "asc",
  "desc" = "desc",
}

export enum TagEnum {
  BUSINESS = "BUSINESS",
  PRODUCT = "PRODUCT",
  OFFER = "OFFER",
}

export enum DurationFilterEnum {
  ALL = "ALL",
  TODAY = "TODAY",
  TOMORROW = "TOMORROW",
  THIS_WEEK = "THIS_WEEK",
  THIS_MONTH = "THIS_MONTH",
}

export enum ActionEnum {
  EDIT = "EDIT",
  PUBLISH = "PUBLISH",
  ARCHIVE = "ARCHIVE",
  DELETE = "DELETE",
  QR_CODE = "QR_CODE",
  APPROVE = "APPROVE",
  REJECT = "REJECT",
}
